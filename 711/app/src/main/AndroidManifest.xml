<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a711">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ChooseADoctorAfterSignup"></activity>
        <activity android:name=".PatientHomePage" />
        <activity android:name=".PatientSignup" />
        <activity android:name=".LoginPage">
            <intent-filter>
                <action android:name="package.LoginPage" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity android:name=".DoctorHomePage" />
    </application>

</manifest>